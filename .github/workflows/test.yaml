name: this is my first github action workflow

on:
    push: 
        branches: main

jobs:
    first job:
        runs-on: ubuntu-latest
        steps:
            - name: hello
              run: echo "hello"
